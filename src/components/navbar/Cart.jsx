import { Grid, Typography } from '@mui/material';

const Cart = () => {
	return (
		<Grid
			container
			sx={{ padding: 1 }}
		>
            <Typography>Carrito de compras</Typography>
        </Grid>
	);
};

export default Cart;
